{% extends 'base.html.twig' %} {% block title 'Agence loren '%} {% block body %}
<div class="jumbotron text-center">
    <h1>
        Agence loren
    </h1>
    <p>
        jdkcjkdljcdsjcldkcxmsj
    </p>
</div>
<div class="container">
    <h2>Les derniers bien</h2>
    <div class="row flex">

        {% for property in properties %}
        <div class="col-3">
            <div class="card">

                <div class="card-body">
                    <h5 class="card-title">
                        <a href="{{ path ('property.show',{id : property.getId(), slug: property.getSlug()}) }}">{{property.title}}</a>
                    </h5>
                    <p class="card_text">{{property.city}} ({{property.postalCode}})</p>
                    <div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{property.getFormattedPrice()}} £</div>
                </div>
            </div>
        </div>

        {% endfor %}
    </div>
</div>
{% endblock %}